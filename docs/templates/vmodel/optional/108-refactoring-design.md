---
template_kind: optional
status: draft
source_id: [ZIP-DOC-108]
source_entries:
  - zip_entry: "vmodel-docgen-clean/templates/108_リファクタリング設計書.yaml"
    sha256: "sha256:a3fabe03164f4cf994f03d78aacc3375da0bc29b70242e5b64814116aaf3f59c"
disposition: "merge → docs/process/modes/refactor.md"
port_index: docs/templates/vmodel/README.md
plan: docs/plans/PLAN-<id>.md
---

# ZIP-DOC-108: リファクタ設計

本テンプレートは `Vモデル設計ドキュメント_checked.zip` (PO 承認、issue #676) の該当 source document を PLAN-L7-676 §3.5.2 の変換規則で Markdown 化した optional テンプレートである (§3.5.3: required slot には束ねない)。項目名・説明文は zip の日本語をそのまま使い、意味は書き換えていない。zip で空欄・空表の箇所には、記入欄の placeholder (`<記入>`、`<本文を記入>`、`<項目を記入>`) だけを置き、記入例の内容は機械生成しない。

## 本文

### 第1章 方針・トリガー

> ※ リファクタリング=外部から観測可能な振る舞いを変えずに内部構造を改善する変更(完全化保守)。機能追加・不具合修正と同一コミットで混ぜない。

#### 1-1 実施トリガー(計測閾値)

| 指標 | 閾値 | 計測手段 | 発火時の扱い |
|---|---|---|---|
|  |  |  |  |

#### 1-2 禁止事項

- 振る舞い変更(仕様変更)をリファクタと称して混入させない(仕様変更は要件改版→通常フロー)
- 対象要件のテストが赤のまま着手しない(まず緑にする)
- 台帳(assign)に記録しない実施

### 第2章 対象と根拠(負債台帳連携)

> ※ 対象は 14_課題・リスク管理 の技術的負債から選定し、根拠(計測値)を必ず添える。「気になるから」は根拠にならない。

| リファクタID | 対象(モジュール/層) | 関連要件 | 根拠(計測値) | 負債票 | 優先 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### 第3章 振る舞い不変の保証(トレース網)

> ※ 本パッケージの中核との接続点。関連要件の宣言済みテスト(spec.defines の tests)が「振る舞い不変」の等価性判定器になる。着手前後で同一テスト集合が緑であることを実行割当台帳(assign)の証跡で示す。

| リファクタID | 等価性テスト(既存宣言) | 追加特性テスト | 判定基準 |
|---|---|---|---|
|  |  |  |  |

#### 3-1 テスト不足時の手順

- 等価性を判定できるテストが無い場合は先に特性テスト(characterization test)を書き06/07へ宣言追加(リファクタ本体より先に台帳でpassにする)
- 特性テストは現状の振る舞いを正とする(仕様の是非は問わない)

### 第4章 戦略・段階計画

| 戦略 | 適用条件 | 段階 |
|---|---|---|
|  |  |  |

| 項目 | 値 |
|---|---|
| RF-001の戦略 | <記入> |
| RF-002の戦略 | <記入> |

### 第5章 切り戻し条件

| 条件 | 判定者 | 切り戻し手順 |
|---|---|---|
|  |  |  |

### 第6章 実施記録

> ※ 実施の事実は実行割当台帳(docs/assign.yaml)の証跡で残す。本章は完了サマリのみ(二重管理しない)。

| リファクタID | 完了日 | 前後計測値 | 等価性判定 | 備考 |
|---|---|---|---|---|
|  |  |  |  |  |
