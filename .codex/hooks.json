{
  "description": "Codex CLI project-local hook adapter (PLAN-L7-139). Official Codex discovery reads <repo>/.codex/hooks.json or <repo>/.codex/config.toml when the project is trusted. This mirrors the Claude guard hooks in .claude/settings.json, wired with Codex tool names, and reuses the same TypeScript entrypoints. Do not move this to global ~/.codex/.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "spawn_agent|spawn_agents_on_csv",
        "hooks": [
          {
            "type": "command",
            "command": "node dist/hooks/agent-guard.mjs",
            "timeout": 5,
            "statusMessage": "agent guard: block non-allowlisted Codex subagent spawns unless explicitly bypassed (Codex parity)",
            "blockOnFailure": true
          }
        ]
      },
      {
        "matcher": "apply_patch|write_file",
        "hooks": [
          {
            "type": "command",
            "command": "node dist/hooks/work-guard.mjs",
            "timeout": 5,
            "statusMessage": "work guard: block edits to foreign uncommitted files unless explicitly bypassed (Codex parity, PLAN-L7-139)",
            "blockOnFailure": true
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node dist/ut-tdd.mjs session start",
            "timeout": 5,
            "statusMessage": "session-log: session start (fail-open)"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "apply_patch|write_file|exec_command|local_shell",
        "hooks": [
          {
            "type": "command",
            "command": "node dist/ut-tdd.mjs hook post-tool-use",
            "timeout": 5,
            "statusMessage": "session-log: post tool use (fail-open)"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node dist/ut-tdd.mjs session summary",
            "timeout": 5,
            "statusMessage": "session-log: PLAN digest summary (fail-open)"
          }
        ]
      }
    ]
  }
}
