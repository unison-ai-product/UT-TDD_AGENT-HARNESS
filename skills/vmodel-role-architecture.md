---
schema_version: skill.v1
name: vmodel-role-architecture
skill_type: reference-example
applies_to:
  layers:
    - L3
    - L4
    - L5
  drive_models:
    - Forward
    - Add-feature
triggers: システムの構造: 方式・構成・ドメイン境界・配置。実装前に全体像を掴むために読む。
---

# vmodel-role-architecture

> 移植元: zip `build/agent/architecture.md` (Apache-2.0、PLAN-L7-676 PR-T3、
> docs/plans/PLAN-L7-676-release-consumer-dev-start.md §3.5.1)。
>
> **これは一般的な行動規約ではなく、サンプル製品「TeamFlow」の設計書 (zip 同梱の
> `docs/*.yaml`) から `python tools/build.py agentdocs` (移植元 `vmodel-docgen` の
> コマンド。`tools/*.py` は本リポジトリへ同梱しない、ADR-001) が自動生成した
> 役割別ダイジェストの実例である。** 本ハーネスではこのコマンドを実行できないため、
> 「実装エージェント向けに、設計書からどのような役割別の要約を作れば実装前に読む
> べき文脈として十分か」を示す worked example として保持する。TeamFlow 固有の
> 数値・命名・業務ルールは当該サンプルのものであり、他プロジェクトへそのまま
> 転用しない。

<!-- 自動生成ファイル: 編集禁止 -->
> **このファイルは docs/*.yaml から生成された読み取り専用ビューです（正本ではありません）。**
> 内容を変えたい場合は対応する docs/*.yaml を編集し、`python tools/build.py agentdocs` で再生成してください。
> 生成: 2026-07-08 / 生成元: 18_アプリケーション方式仕様書, 04_基本設計書, 27_ドメインモデル設計書, 45_ディレクトリ構成・プロジェクト構造設計, 25_ネットワーク設計書, 26_サーバー・インフラ設計書, 72_フロントエンド設計書, 76_モバイルアプリアーキ設計書, 82_デスクトップアプリアーキ設計書, 88_CLIアーキ・コマンド体系設計書

**目的**: システムの構造: 方式・構成・ドメイン境界・配置。実装前に全体像を掴むために読む。

### TeamFlow / アプリケーション方式仕様書


### 第1章 実行環境

| 項目 | 内容 |
| --- | --- |
| 提供形態 | マルチテナントSaaS(クラウド) |
| 対応ブラウザ | 最新版 Chrome/Edge/Safari/Firefox |
| 実行基盤 | マネージドコンテナ＋オートスケール |
| 環境 | dev / stg / prod(本番同等stg) |

### 第2章 共通方式(文字コード等)

| 項目 | 方式 |
| --- | --- |
| 文字コード | UTF-8(内部/API/ファイルBOMなし) |
| タイムゾーン | 保存はUTC、表示は利用者ローカル |
| 日付書式 | ISO 8601(表示は地域書式) |
| 数値/通貨 | 通貨は最小単位(整数)で保持、表示時整形 |
| 国際化 | i18nキーで多言語(和/英)対応 |

### 第3章 セッション/状態管理

- 対人はJWT/セッション、公開APIはAPIキー/OAuth2
- JWTにtenant_id・roleを内包し、無操作タイムアウトを設定
- 状態はサーバ側で持たずステートレス(水平スケール前提)

### 第4章 ログ出力方式

- 構造化ログ(JSON)。相関ID(request_id)・tenant_idを付与
- レベル DEBUG/INFO/WARN/ERROR、個人情報/秘密はマスキング
- 集約基盤へ転送し、メトリクス・トレースと相関(詳細は運用設計書)

> ※ ログレベル・エラーコード体系は『開発標準・規約書』第4章に準拠。

### 第5章 共通処理方式

| 方式 | 内容 |
| --- | --- |
| ページング | 一覧はカーソル/リミット方式(既定50・最大200) |
| 排他制御 | 更新は楽観ロック(updated_at/version) |
| 監査 | 重要操作は監査ログ(F-009)へ記録 |
| 例外/リトライ | 開発標準の例外・リトライ標準に準拠 |


---

### TeamFlow / 基本設計書


### 第1章 システム構成(SaaSアーキ)

#### 1-1 構成方針

CDN→ロードバランサ→ステートレスなAppサーバ群→共有RDB(tenant_id分離)の構成。非同期処理はメッセージキュー、決済はStripe、認証はIdP(SSO)と連携するクラウドネイティブSaaS。

#### 1-2 構成要素

| No | 層/要素 | 役割 | 技術例 |
| --- | --- | --- | --- |
| 1 | CDN/WAF | 配信高速化・防御 | マネージドCDN |
| 2 | ロードバランサ | 分散・冗長化 | マネージドLB |
| 3 | Appサーバ(stateless) | API/画面処理 | Web APフレームワーク |
| 4 | RDB(共有) | 業務データ(tenant_id) | マネージドRDB+RLS |
| 5 | キュー/ワーカー | 通知・集計の非同期 | マネージドキュー |
| 6 | オブジェクトストレージ | 添付ファイル | マネージドストレージ |
| 7 | 外部SaaS | 決済/SSO/監視 | Stripe/IdP/監視基盤 |

### 第2章 テナント分離設計

#### 2-1 分離方式

共有DB・共有スキーマ方式を採用し、全業務テーブルにtenant_id列を持たせ、行レベルセキュリティ(RLS)でセッションのtenant_id以外を遮断する。アプリ層でも全クエリにtenant_id条件を強制する二重防御とする。

#### 2-2 分離の担保

- 接続時にセッション変数へtenant_idを設定しRLS適用
- 共通リポジトリ層でtenant_idを自動付与
- 分離違反を検知する自動テストをCIで実行

### 第3章 機能概要（要件は03/詳細は05）

> ※ トレース元は要件の機能要件(F-xxx)。機能IDは詳細設計・単体テストへ継承。

| 機能ID | 大機能 | 機能名 | 処理概要 | トレース元 |
| --- | --- | --- | --- | --- |
| F-001 | テナント | 登録/オンボーディング | 組織作成・初期設定 | F-001(要件) |
| F-002 | 認証 | 認証/SSO | メール/SAML/OIDC | F-002(要件) |
| F-003 | 組織 | ユーザー/ロール管理 | 招待・RBAC | F-003(要件) |
| F-004 | 業務 | プロジェクト/タスク管理 | CRUD・カンバン | F-004(要件) |
| F-005 | 通知 | 通知 | メール/Webhook | F-005(要件) |
| F-006 | 可視化 | ダッシュボード | 集計・グラフ | F-006(要件) |
| F-007 | 課金 | サブスク課金 | Stripe連携 | F-007(要件) |
| F-008 | 連携 | 公開API | REST/APIキー | F-008(要件) |
| F-009 | 統制 | 監査ログ | 操作記録 | F-009(要件) |

### 第4章 画面一覧

| 画面ID | 画面名称 | 概要 | 関連機能 | ロール |
| --- | --- | --- | --- | --- |
| SC-001 | サインアップ/オンボーディング | 組織作成・初期設定 | F-001 | 管理者 |
| SC-002 | ログイン(SSO) | 認証 | F-002 | 全員 |
| SC-003 | プロジェクト一覧 | PJの一覧・作成 | F-004 | メンバー |
| SC-004 | タスクボード(カンバン) | タスクの管理 | F-004 | メンバー |
| SC-005 | ダッシュボード | 進捗/遅延の可視化 | F-006 | 管理者 |
| SC-006 | メンバー/権限管理 | 招待・RBAC | F-003 | 管理者 |
| SC-007 | 課金・プラン設定 | プラン/支払い | F-007 | 管理者 |

### 第5章 画面遷移

#### 5-1 遷移概要

サインアップ(SC-001)またはログイン(SC-002)後、プロジェクト一覧(SC-003)を起点に、タスクボード(SC-004)/ダッシュボード(SC-005)/メンバー管理(SC-006)/課金(SC-007)へ遷移する。

#### 5-2 主要遷移

| No | 遷移元 | 操作 | 遷移先 |
| --- | --- | --- | --- |
| 1 | SC-002 ログイン | 認証成功 | SC-003 PJ一覧 |
| 2 | SC-003 PJ一覧 | PJ選択 | SC-004 タスクボード |
| 3 | SC-003 | [メンバー] | SC-006 メンバー管理 |
| 4 | SC-005 | [プラン変更] | SC-007 課金設定 |

### 第6章 データモデル(テーブル一覧)

> ※ 全テーブルは tenant_id を保持。物理列は詳細設計で確定。

| テーブルID | 論理名 | 物理名 | 種別 | 主な関連 |
| --- | --- | --- | --- | --- |
| T-001 | テナント | tenants | マスタ | - |
| T-002 | ユーザー | users | マスタ | T-001 |
| T-003 | プロジェクト | projects | トランザクション | T-001,T-002 |
| T-004 | タスク | tasks | トランザクション | T-003,T-002 |
| T-005 | サブスクリプション | subscriptions | トランザクション | T-001 |
| T-006 | 監査ログ | audit_logs | ログ | T-001,T-002 |

### 第7章 CRUDマトリクス

> ※ 機能(F-xxx)×テーブル(T-xxx)のCRUD分析。C=生成 / R=参照 / U=更新 / D=削除。空欄=アクセスなし。

| 機能 | tenants | users | projects | tasks | subscriptions | audit_logs |
| --- | --- | --- | --- | --- | --- | --- |
| F-001 テナント登録 | C | CU |  |  | C | C |
| F-002 認証/SSO | R | RU |  |  |  | C |
| F-003 ユーザー/ロール管理 | R | CRUD |  |  |  | C |
| F-004 プロジェクト/タスク |  | R | CRUD | CRUD |  | C |
| F-005 通知 |  | R | R | R |  | C |
| F-006 ダッシュボード |  | R | R | R | R |  |
| F-007 サブスク課金 | U |  |  |  | CRUD | C |
| F-008 公開API |  | R | CRUD | CRUD |  | C |
| F-009 監査ログ |  |  |  |  |  | R |

> ※ 全アクセスは自テナント(tenant_id)スコープに限定される。

### 第8章 API設計一覧

| API-ID | メソッド/パス | 概要 | 認証 | トレース元 |
| --- | --- | --- | --- | --- |
| API-001 | GET/POST /v1/projects | PJ一覧/作成 | APIキー | F-008 |
| API-002 | GET/POST/PATCH /v1/tasks | タスク取得/作成/更新 | APIキー | F-008 |
| API-003 | POST /v1/webhooks | Webhook登録 | APIキー | F-005 |
| API-004 | POST /stripe/webhook | 決済イベント受信 | 署名検証 | F-007 |

### 第9章 非同期/バッチ

| ID | 処理名 | 契機 | 概要 | 関連 |
| --- | --- | --- | --- | --- |
| BT-001 | 通知配信 | イベント発生 | メール/Webhook送信(再試行) | F-005 |
| BT-002 | 使用量集計 | 日次 2:00 | テナント別の利用量集計 | F-007 |
| BT-003 | 請求確定/同期 | 月次 1日 | Stripeと請求整合 | F-007 |
| BT-004 | バックアップ | 日次 | DBスナップショット取得 | NF-006 |

### 第10章 認証・認可/非機能設計

#### 9-1 認証・認可

- セッション/JWT＋テナントスコープ
- RBAC(admin/member/viewer)をミドルウェアで強制
- 公開APIはAPIキー、対人はSSO/OIDC

#### 9-2 非機能設計方針

| ID | 分類 | 設計方針 | トレース元 |
| --- | --- | --- | --- |
| D-NF-001 | 分離 | RLS＋リポジトリ層でtenant_id強制 | NF-001 |
| D-NF-002 | 可用性 | マルチAZ、ヘルスチェック、自動復旧 | NF-002 |
| D-NF-003 | スケール | ステートレス化+オートスケール | NF-003 |
| D-NF-004 | 可観測性 | 監査ログ/メトリクス/トレース | NF-007 |


---

### TeamFlow / ドメインモデル設計書(DDD)


### 第1章 方針・ユビキタス言語

- DDDの戦術/戦略パターンでドメインを構造化し、実装・テストと用語を揃える
- ユビキタス言語を要件・設計・BDDシナリオ・コードで共通利用

#### 1-1 ユビキタス言語(抜粋)

| 用語 | 定義(ドメイン) |
| --- | --- |
| テナント | 契約・分離の境界となる組織 |
| プロジェクト | タスクを束ねる作業単位 |
| タスク | 担当・期限・状態を持つ作業項目 |
| サブスクリプション | テナントの課金契約(状態を持つ) |
| ドメインイベント | 業務上意味のある出来事(過去形) |

### 第2章 境界づけられたコンテキスト

> ※ 各コンテキストは独立して進化。統合はイベント/契約(API)で疎結合。

| コンテキスト | 責務 | 主な集約 |
| --- | --- | --- |
| Identity & Access | 認証・テナント・ユーザー・権限 | Tenant, User |
| Project/Task | プロジェクト/タスク管理 | Project, Task |
| Billing | 課金・契約・請求 | Subscription |
| Notification | 通知配信 | -(方針) |
| Audit | 監査ログ | AuditLog |

### 第3章 コンテキストマップ

> ※ 図『コンテキストマップ』(図面集)参照。関係パターン: 上流/下流, ACL(腐敗防止層), 公開言語(PL)。

| 上流→下流 | 関係 | 連携方式 |
| --- | --- | --- |
| Identity→Project/Task | Customer/Supplier | JWT(tenant/role) |
| Project/Task→Notification | Publisher/Subscriber | ドメインイベント |
| Billing→Identity | Conformist | プラン上限反映 |
| Billing→外部(Stripe) | ACL(腐敗防止層) | Webhook冪等 |

### 第4章 集約(Aggregate)一覧

> ※ 集約ルート単位で一貫性(不変条件)を保つ。トランザクション境界=集約。

| 集約ルート | 構成(エンティティ/VO) | 不変条件(invariant) |
| --- | --- | --- |
| Tenant | User, Plan(VO) | email はテナント内一意 |
| Project | Task, Deadline(VO) | タスクは自PJ配下のみ |
| Task | Status(VO), Assignee | status遷移は許可経路のみ |
| Subscription | Status(VO), Seats(VO) | 支払い状態と上限が整合 |

### 第5章 エンティティ/値オブジェクト

> ※ 実装方針(完全コンストラクタ・不変・値オブジェクトカタログ・生成/再構築・AIコード生成コントラクト)は「94_ドメイン実装方針・値オブジェクト設計」を参照。

| 種別 | 名称 | 説明 |
| --- | --- | --- |
| Entity | User | 同一性を持つ利用者 |
| Entity | Task | 状態を持つ作業項目 |
| VO | TaskStatus | todo/doing/done(不変) |
| VO | Money | 通貨×最小単位(不変) |
| VO | DateRange | 契約期間(不変) |

### 第6章 ドメインイベント

> ※ イベントは過去形。通知/計測/監査の起点(Publisher/Subscriber)。

| イベント | 発生集約 | 主な購読 |
| --- | --- | --- |
| TenantRegistered | Tenant | Notification, 計測 |
| TaskAssigned | Task | Notification(MAIL-03) |
| SubscriptionActivated | Subscription | Identity(上限反映) |
| PaymentFailed | Subscription | Notification(MAIL-04), 運用 |

### 第7章 リポジトリ/ドメインサービス

- 集約ごとにリポジトリを定義し、tenant_idスコープを強制
- 集約をまたぐ処理はドメインサービス(例: 課金→上限反映)
- 永続化/外部連携はインフラ層に隔離(ヘキサゴナル)


---

### TeamFlow / ディレクトリ構成・プロジェクト構造設計


### 第1章 方針

- ヘキサゴナル(ドメイン中心)をディレクトリ構造に反映
- 境界づけられたコンテキスト単位でモジュール分割
- 依存は内向き(ドメインは外部に依存しない)を構造で強制

### 第2章 リポジトリ構成

- モノレポを基本(apps/ と packages/ を分離)
- フロント/バック/共通パッケージを1リポジトリで管理し整合を担保
- IaC・ドキュメント・BDD仕様も同居しコードと同期

### 第3章 ソースディレクトリ構成

teamflow/                      # モノレポ<br>├─ apps/<br>│  ├─ web/                     # フロント(SSR/SSG: 公開ページ＋アプリ)<br>│  └─ api/                     # バックエンドAPI<br>├─ packages/<br>│  ├─ domain/                  # ドメイン層(集約/エンティティ/VO/イベント)<br>│  ├─ application/             # ユースケース<br>│  ├─ infrastructure/          # 永続化/外部連携アダプタ<br>│  └─ shared/                  # 共通部品(CMN-*)<br>├─ services/                   # 非同期ワーカー/バッチ(BT-*)<br>├─ infra/                      # IaC(環境定義)<br>├─ tests/                      # テスト<br>├─ specs/                      # BDD .feature<br>├─ docs/                       # 設計ドキュメント<br>└─ config/                     # 環境別設定(外部化)

### 第4章 レイヤ↔ディレクトリ対応

| レイヤ | ディレクトリ | 含むもの |
| --- | --- | --- |
| プレゼン | apps/web, apps/api | 画面/API入出力 |
| アプリ | packages/application | ユースケース |
| ドメイン | packages/domain | 集約/エンティティ/VO/イベント |
| インフラ | packages/infrastructure | リポジトリ実装/外部アダプタ |
| 共通 | packages/shared | 共通部品(CMN-*) |

### 第5章 命名規約

- ディレクトリ/ファイルはケバブ or スネークで統一(言語慣習に従う)
- コンテキスト単位でサブディレクトリを切る(例: domain/billing)
- 1ファイル1責務、公開APIはindexで明示

### 第6章 設定・テスト・ドキュメント配置

- 設定は config/ と環境変数に外部化(コードに埋め込まない)
- 秘密はコード外(シークレット管理)、参照のみ
- テストは tests/(または各パッケージ併設)、BDDは specs/
- IaCは infra/、ドキュメントは docs/(本リポジトリ)

### 第7章 依存の方向

- 依存は presentation→application→domain の内向きのみ
- infrastructure は domain のポート(I/F)を実装(依存を反転)
- 循環依存を禁止し、Lint/CIで構造を検査(依存境界を強制)


---

### TeamFlow / ネットワーク設計書


### 第1章 方針

- 多層防御・最小開放(必要な通信のみ許可)
- 全通信TLS化、内部通信も暗号化を基本
- 公開面はCDN/WAF、アプリ/DBはプライベート配置

### 第2章 ネットワークセグメント

> ※ デプロイ・ネットワーク構成図(図面集)と対応。マルチAZ。

| セグメント | 配置 | 公開 | 主な要素 |
| --- | --- | --- | --- |
| Public Subnet | 各AZ | 外向き | ALB, NAT GW |
| Private Subnet | 各AZ | 非公開 | App(Auto Scaling) |
| Data Subnet | 各AZ | 非公開 | RDB, Cache |

### 第3章 通信要件(ポート/プロトコル)マトリクス

| 送信元 | 宛先 | プロト/ポート | 用途/暗号 |
| --- | --- | --- | --- |
| Internet | ALB | TCP/443 | HTTPS(TLS) |
| ALB | App | TCP/8443 | HTTPS(内部) |
| App | RDB | TCP/5432 | SQL(TLS) |
| App | Cache | TCP/6379 | キャッシュ(TLS) |
| App | Internet(Stripe/IdP) | TCP/443 | 外部API(TLS,egress) |

### 第4章 ファイアウォール/SGルール

| ルールID | 方向 | 送信元→宛先 | ポート | 動作 |
| --- | --- | --- | --- | --- |
| SG-01 | IN | 0.0.0.0/0 → ALB | 443 | 許可 |
| SG-02 | IN | ALB → App | 8443 | 許可 |
| SG-03 | IN | App → RDB | 5432 | 許可 |
| SG-04 | OUT | App → Stripe/IdP | 443 | 許可 |
| SG-99 | IN | その他すべて | * | 拒否 |

### 第5章 DNS・証明書(TLS)

- 独自ドメインをDNSで管理(app./api.)、CDN経由で配信
- TLS証明書はマネージドで自動更新、TLS1.2+ / HSTS有効
- 内部サービスは相互TLS(mTLS)を検討

### 第6章 LB/CDN/WAF

- ALBで分散・ヘルスチェック・AZ冗長
- CDNで静的配信高速化、WAFでOWASP系攻撃を遮断
- レート制限・IPレピュテーションで乱用防止

### 第7章 外部接続

| 接続先 | 方向 | 方式 | 制御 |
| --- | --- | --- | --- |
| Stripe | egress | HTTPS/Webhook | 署名検証/IP許可 |
| IdP(SSO) | 双方向 | SAML/OIDC | 証明書/メタデータ |


---

### TeamFlow / サーバー・インフラ設計書


### 第1章 方針

- クラウドのマネージドサービスを基本とし運用負荷を最小化
- アプリはステートレス化して水平スケール
- 環境はIaCで再現可能に管理

### 第2章 サーバ/コンポーネント一覧

| 名称 | 役割 | 台数/冗長 | スケール |
| --- | --- | --- | --- |
| ALB | 負荷分散 | マネージド/AZ冗長 | 自動 |
| App(コンテナ) | API/画面処理 | N+1(最低2AZ) | オートスケール |
| Worker | 非同期/バッチ | N+1 | キュー連動 |
| RDB | データ | Primary+Replica(別AZ) | 垂直＋リプリカ |
| Cache | キャッシュ/セッション補助 | 冗長構成 | - |

### 第3章 ミドルウェア一覧

| ミドルウェア | 用途 | バージョン方針 |
| --- | --- | --- |
| Webアプリランタイム | API/画面 | LTSを追随 |
| RDBMS | 業務データ(RLS対応) | マネージド最新安定 |
| メッセージキュー | 非同期処理 | マネージド |
| キャッシュ | 高速化 | マネージド |
| 監視/ログ基盤 | 可観測性 | SaaS/マネージド |

### 第4章 キャパシティ・サイジング

- 1台停止(N+1)でも全負荷を捌ける余力を確保
- 想定同時接続・API RPSからApp台数を算定、CPU70%で増設
- DBは接続数・IOPS・ストレージ成長を見積り、リプリカで読み分散
- 四半期ごとに負荷試験で余力を検証(運用設計と連携)

### 第5章 冗長化・可用性

- マルチAZでSPOF(単一障害点)を排除
- ヘルスチェック＋自動フェイルオーバー(App/DB)
- 無停止デプロイ(ブルーグリーン/カナリア)
- 目標可用性99.9%(運用設計のSLOに準拠)

### 第6章 スケーリング

| 対象 | 方式 | トリガ |
| --- | --- | --- |
| App/Worker | 水平(オートスケール) | CPU/キュー長 |
| RDB | 垂直＋リードレプリカ | 負荷/接続数 |
| Cache | クラスタ拡張 | メモリ/ヒット率 |

### 第7章 構成管理・IaC

- インフラはIaCでコード管理、変更はPRレビュー＋CI
- dev/stg/prodを同一定義から再現、秘密はシークレット管理
- 構成変更は変更管理・監査ログに記録


---

### TeamFlow / フロントエンド設計(SPA/SSR・ルーティング・状態管理)


### 第1章 方針

> ※ TeamFlowのWebクライアント層。画面(SC-xxx)を実装するフロントエンドの構成を定義。

- コンポーネント分割と再利用を基本に据える
- 描画方式(SPA/SSR/SSG)を要件で使い分ける
- 状態はローカル/サーバ状態を分離して管理

### 第2章 描画方式

| 方式 | 用途 | 採用 |
| --- | --- | --- |
| SPA | 操作主体の管理画面 | 主要画面 |
| SSR | 初期表示/SEO重視 | 公開ページ(46) |
| SSG | 静的コンテンツ | ヘルプ等 |

### 第3章 ルーティング/画面

| ルート | 画面 | 認可 |
| --- | --- | --- |
| /dashboard | SC-005 | ログイン必須 |
| /tasks | SC-004 | メンバー |
| /members | SC-006 | 管理者 |
| /settings | SC-007 | 管理者 |

### 第4章 状態管理

| 状態種別 | 手段 |
| --- | --- |
| サーバ状態 | データ取得キャッシュ/再検証 |
| UI状態 | コンポーネントローカル |
| 共有状態 | ストア(最小限) |

### 第5章 API連携/データ取得

> ※ バックエンドAPI(API-xxx)を型付きクライアントで呼び出す。エラー/ローディングを共通化。

| 対象 | 取得方式 |
| --- | --- |
| タスク一覧(F-004) | ページング取得 |
| ダッシュボード(F-006) | 集計取得/キャッシュ |
| 公開API(F-008) | トークン付与 |

### 第6章 関連設計・トレース

| 関連ID | 名称 | 関係 |
| --- | --- | --- |
| SC-004 | タスクボード | 主要画面の実装 |
| F-006 | ダッシュボード | 集計表示 |
| NF-005 | 性能 | 描画/取得の応答目標 |


---

### TeamFlow / モバイルアプリアーキ設計(MVVM・ナビゲーション)


### 第1章 方針

> ※ TeamFlowのモバイルクライアント。画面(SC-xxx)/機能(F-xxx)をネイティブ体験で提供。

- ネイティブ/クロスプラットフォームを要件で選定
- プレゼンテーションは MVVM 等で分離
- ドメイン/データ層を疎結合に保つ

### 第2章 アーキ層

| 層 | 責務 |
| --- | --- |
| Presentation | 画面/ViewModel(MVVM) |
| Domain | ユースケース/エンティティ |
| Data | リポジトリ/ローカル(77)/API |

### 第3章 ナビゲーション/画面遷移

| 画面 | 遷移/役割 |
| --- | --- |
| SC-004 タスク | 一覧→詳細→編集 |
| SC-005 ダッシュボード | タブ起点 |
| SC-006 メンバー | 管理者導線 |

### 第4章 状態管理/DI

- 単方向データフローで状態を予測可能に
- 依存性注入でテスト容易性を確保
- 画面回転/プロセス終了時の状態復元

### 第5章 関連設計・トレース

| 関連ID | 名称 | 関係 |
| --- | --- | --- |
| SC-004 | タスクボード | 主要画面 |
| F-004 | プロジェクト/タスク管理 | 主要機能 |
| F-006 | ダッシュボード | 集計表示 |


---

### TeamFlow / デスクトップアプリアーキ設計


### 第1章 方針

> ※ TeamFlowのデスクトップクライアント。Web資産の再利用とネイティブ統合(86)を両立する。

- Web/ネイティブ資産の再利用度で技術を選定
- メイン/レンダラのプロセス分離で安全性を確保
- オフライン(87)とOS統合(86)を前提に設計

### 第2章 技術選定

| 選択肢 | 特徴 | 適合 |
| --- | --- | --- |
| Electron | Web資産流用/配布容易 | 短期立上げ |
| Tauri | 軽量/Rustコア/小サイズ | 省リソース |
| ネイティブ | OS最適/高性能 | 高度統合 |

### 第3章 プロセス/ウィンドウ/IPC

| 要素 | 責務 |
| --- | --- |
| メインプロセス | ライフサイクル/OS API/権限 |
| レンダラ | UI描画(フロントエンド72流用) |
| IPC | 最小権限の橋渡し/検証 |

### 第4章 画面/機能マッピング

| 画面 | 役割 |
| --- | --- |
| SC-004 タスクボード | 主作業画面 |
| SC-005 ダッシュボード | 常駐サマリ |

### 第5章 関連設計・トレース

| 関連ID | 名称 | 関係 |
| --- | --- | --- |
| SC-004 | タスクボード | 主要画面 |
| F-006 | ダッシュボード | 常駐表示 |
| NF-005 | 性能 | 起動/描画の応答 |


---

### TeamFlow / CLIアーキ・コマンド体系設計


### 第1章 方針

> ※ 管理者/CI向けCLI。公開API(F-008/API-xxx)を土台に、自動化しやすい一貫した体系にする。

- 名詞+動詞のサブコマンド体系で直感的に
- 非対話/CIでの利用を第一級で支援
- 権限(F-003)に応じた操作範囲

### 第2章 コマンド体系

| サブコマンド | 用途 |
| --- | --- |
| auth | ログイン/トークン管理 |
| tenant | テナント管理(管理者) |
| task | タスクのCRUD(F-004) |
| export | データエクスポート |

### 第3章 引数/オプション規約

| 規約 | 内容 |
| --- | --- |
| グローバル | --profile/--output/--quiet |
| 確認 | 破壊的操作は--yesで抑制 |
| 冪等 | 再実行安全なフラグ設計 |

### 第4章 終了コード/標準入出力

| コード | 意味 | 出力 |
| --- | --- | --- |
| 0 | 成功 | stdoutに結果 |
| 1 | 一般エラー | stderrに詳細 |
| 2 | 入力誤り | usage表示 |
| 4 | 認証/権限 | 再認証誘導 |

### 第5章 関連設計・トレース

| 関連ID | 名称 | 関係 |
| --- | --- | --- |
| F-008 | 公開API | CLIの土台 |
| API-002 | タスクAPI | task操作の実体 |
| F-003 | ユーザー/ロール管理 | 操作権限 |
