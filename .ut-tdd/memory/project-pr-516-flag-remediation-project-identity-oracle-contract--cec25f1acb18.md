---
memory_id: memory:project:pr-516-flag-remediation-project-identity-oracle-contract--cec25f1acb18
kind: project
title: "PR #516 FLAG remediation: project identity oracle contract"
tags: []
updated_at: 2026-09-04T04:07:32.108Z
---

PR #516 (Issue #432, exact HEAD 6af3e8db6b19cdd4933e53197c21b365456db0d2, base b27720644a7589dc568c76cad7eb5e068654c824) Codex/Sol exact-head receipt 970442b00fe129c5e63a0b82d14ebf84535eb9ada9fee385a411baf347d45c08 is FLAG / blocking 4. Claude-authored docs-only pair-freeze; do not weaken or self-edit from Codex. Remediate in the owning contract lane, then new exact-head CI and Codex closing review. Findings: (1) Forward §1/§4 and Issue #432 AC require working-tree drift typed deny, but Forward §3.1, Reverse candidate 002 and test-design CANDIDATE-U-PROJID-002 say the loader accepts HEAD value without detecting working-tree modification. Make the contract/oracle consistent; unverified working-tree bytes must never be authoritative. (2) HEAD drift negative only tests a later reread; loadProjectIdentityFromHead performs rev-parse HEAD, ls-tree HEAD and show HEAD:path separately without an atomic subject/ref fence or sourceCommit/blob same-commit binding, so TOCTOU can be accepted. (3) CRLF mutation oracle claims digest mismatch, but current loader derives contentDigest/blobOid from current HEAD bytes and validates those same bytes; a valid CRLF commit can be accepted. Reword the claim or freeze a real normalization/encoding deny contract with an executable oracle. (4) stale identity owner/repository binding is optional expectedRepositoryIdentity; node-plan-revision-runner, legacy-plan-inventory and project-memory-root do not pass an origin-derived expected identity, so a grammar-valid identity copied from another repository can become authoritative. Define the trust root and require binding on every read path. Preserve docs-only scope only if these are contract corrections; otherwise split implementation prerequisites. Do not claim #424 E2E or #463/#515 completion.
