---
memory_id: memory:feedback:pr-497-issue-454-wake-liveness-contract-merged-at-48ef1cf8
kind: feedback
title: "PR 497 issue 454 wake liveness contract merged at 48ef1cf8"
tags: ["issue-454", "pr-497", "review-receipt"]
updated_at: 2026-08-31T11:22:42.641Z
---

PR #497 (issue #454 Claude wake liveness contract, docs-only) merged at exact head 48ef1cf8 via ut-tdd pr merge. Canonical cross_agent receipts: 16074a0f at 794b5279 (full review, PASS-WEAK blocking 0) then a51ddcfe at 48ef1cf8 (rebase-delta only). My earlier FLAG/5 (b969bc8b at 1d265c6a) fully closed: (1) deny path now persists the canonical request as backlog and zeroes only downstream writes, preserving the very channel that made #454 observable; (2) U-MEMWAKE-007 explicitly declared as NOT silently reversed; (3) deferred queue lifecycle fully frozen - runtimeRoot/deferred/<key>.json + deferred/promoted/<key>.json, closed entry schema, consumer at SessionStart/Stop wake boundary with generation identity verification, 7-day GC that explicitly scans deferred/ and is NOT mixed into the inbox glob GC (this was the pruneRuntimeFiles collision I flagged); (4) corrupt and identity-unverifiable collapse into incompatible_claude_workspace_schema so the taxonomy is 1:1 with the existing 4 reasons, and all mixed states enumerated; (5) exact-one counts LOGICAL workspaces grouped by canonical workspaceId with fresh-wins, so the real 3-markers-one-workspaceId state is logical count 1 and does not fall into permanent deny. 794b5279 additionally froze durable timestamp reuse (createdAt/eligibleAfter/promotedAt never re-minted from retry wall clock, across parallel losers, inbox-first recovery, and missing promotion marker). Rebase onto f38b78d8 (#496) verified as content-identical by blob OID for 3 of 4 files; the L7-unit-test-design delta was #496's own U-HOOKEXEC-002/008 rewrite. No oracle ID collision: U-MEMWAKE-007 appears once as definition plus two references. PASS-WEAK because implementation does not exist yet; the impl PR must fix as green oracles (a) deferred GC separated from pruneRuntimeFiles glob and (b) logical aggregation returning live for 3 markers sharing one workspaceId.
