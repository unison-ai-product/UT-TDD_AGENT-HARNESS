#!/bin/bash
# DEPRECATED: このパスは後方互換のため残されています。
# 新しい参照は cli/libexec/helix-session-start を使ってください。
# 将来のリリースで削除される可能性があります。
set -e
REAL_DIR="$(cd "$(dirname "$0")" && pwd)"
if [[ -z "${HELIX_SUPPRESS_LEGACY_WARN:-}" ]]; then
  echo "[helix] warn: cli/helix-session-start は deprecated。cli/libexec/helix-session-start を参照してください (HELIX_SUPPRESS_LEGACY_WARN=1 で抑止)" >&2
fi
exec "$REAL_DIR/libexec/helix-session-start" "$@"
