#!/usr/bin/env sh
# UT-TDD thin POSIX entrypoint (ADR-001 / PLAN-L7-507). Node runs the TypeScript CLI directly.
set -e
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
exec node "$ROOT/src/cli.ts" "$@"
